# models

Place `chair.glb` here before running the application.

The file is not committed to the repository. Obtain it from the design team and copy it to this directory:

```
public/models/chair.glb
```

The model is referenced by `src/data/chairModel.ts` → `src: '/models/chair.glb'`.
